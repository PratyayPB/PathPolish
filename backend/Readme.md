# MODELS: Store the DB Schema

# CONFIG: Setup and Connections to external entities like DB and APIs

# ROUTES: Stores the routes for every page

# CONTROLLER: Core logic for every route(module);

# MIDDLEWARE: Store reusable middlewares like authentication, error handling, and input validation.

# SERVICES: Core logic for external entities like Gemini, PDF Parsing, Mermaid Conversion etc.

# UTILS:

# Server.js: Entry point,runs server

# App.js: Express app confign

# FLOW: server.js ➜ app.js ➜ config/ ➜ routes/ ➜ controllers/ ➜ models/ ➜ services/utils/

### DEPENDENCIES: mongoose,express,dotenv,cors,nodemon,body-parser,morgan(request logging),@google/genai,@google/generative-ai,@mermaid-js/mermaid-cli(server testing),path,ejs, bcrypt, passport ,passport-local, passport-google-oauth20, passport-github2 ,express-session, connect-mongo,nanoid

,
